import { Identifier } from './syntax/syntax'

export const requireIdentifier: Identifier = {
    type: 'Identifier',
    name: 'require'
}

export const jsonServerIdentifier: Identifier = {
    type: 'Identifier',
    name: 'jsonServer'
}

export const fsIdentifier: Identifier = {
    type: 'Identifier',
    name: 'fs'
}

export const serverIdentifier: Identifier = {
    type: 'Identifier',
    name: 'server'
}

export const discardIdentifier: Identifier = {
    type: 'Identifier',
    name: '_'
}
